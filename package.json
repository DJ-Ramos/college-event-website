{
  "name": "college-event-website",
  "version": "0.1.0",
  "engines": {
    "node": "16.17.0",
    "npm": "9.6.3"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "build": "cd server && npm install && node index.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}
